services:
  - type: web
    name: medivision-xr-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python deploy.py
    envVars:
      - key: PORT
        value: 10000
